# Extended Scope

While editors and widgets are mostly allowed to display and edit data specified by their path configuration, specific use cases (such as conditionally displaying a widget) require extended access to other data elements.&#x20;
